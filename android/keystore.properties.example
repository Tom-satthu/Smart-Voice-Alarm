# Copy this file to keystore.properties (do not commit the real file).
# Place your upload keystore outside the repository when possible.

storePassword=REPLACE_ME
keyPassword=REPLACE_ME
keyAlias=upload
storeFile=/absolute/path/to/smart-voice-alarm-upload.jks
